<!doctype html>
<html>
<head>
    <title> My Personal Wishlist </title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <link rel="stylesheet" href="style.css">
</head>
<body>

<div class="count"> <a href="/cart"><%=cart.length%></a></div>
<div class="container">

    <div class="page-header text-center">
    <h1>  My Personal Wishlist  </h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
    </div>

    <div class="row">

        <!-- LOCAL INFORMATION -->

        <div class="col-sm-6">
        <img src="img/wishlist.png">
        </div>

        <div class="col-sm-6">
          <h3> Create your Wishlist Down Below</h3>
          <ul class="messages">
          <% for(var i=0; i<jewelry.length; i++) {%>
            <li class="message">
              <form action="/add" method="post">
              <input name="title" value="<%= jewelry[i].title %>" readonly>
              <input name="price" value="$<%= jewelry[i].price %>" readonly>
              <input name="description" value="<%= jewelry[i].description %>" readonly> 
              <div>
              <% for(var s = 0; s < jewelry[i].rating; s++) {%>
                <span>⭐️</span>
              <%} %>
              </div>
              <button class="add" type="submit"> 🛒 </button> 
            </form>

            </li>
          <% } %>
          </ul>


</div>

<script src="main.js"></script>
<script src="server.js"></script>

</body>
</html>
